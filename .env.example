# Local: full URL to FastAPI (see README).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
#
# Vercel (Services + vercel.json): Vercel injects NEXT_PUBLIC_API_URL for the
# `api` service (route prefix below). Override only if needed:
# NEXT_PUBLIC_API_URL=/_backend
