# CPU-only PyTorch — avoids ~2GB+ of CUDA/NVIDIA wheels on Linux (e.g. Render).
# Default `pip install torch` on x86_64 Linux pulls GPU stacks and often OOMs small instances.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

fastapi==0.115.6
uvicorn[standard]==0.32.1
sentence-transformers==3.3.1
rapidfuzz==3.14.5
scikit-learn==1.5.2
numpy==2.1.3
pytest==8.3.3
httpx==0.27.2
